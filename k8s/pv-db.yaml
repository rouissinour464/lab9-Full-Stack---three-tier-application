apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-db
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/lab5db   # dossier sur ton node pour stocker les données
  persistentVolumeReclaimPolicy: Retain
